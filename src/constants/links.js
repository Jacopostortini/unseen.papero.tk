export default [
    {
        from: 1, //the highest (or the one on the left)
        to: 8, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[3,2]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 1, //the highest (or the one on the left)
        to: 9, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[10,2]],
        type: 0/*5*///0,1,2,3,4 TODO
    },
    {
        from: 2, //the highest (or the one on the left)
        to: 10, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[20,3]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 2, //the highest (or the one on the left)
        to: 20, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[14,3],[14,6]],
        type: 0 //0,1,2,3,4
    },
/*    {
        from: 3, //the highest (or the one on the left)
        to: 4, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[23,0],[27,0]],
        type: 0 //0,1,2,3,4
    },*/
    {
        from: 3, //the highest (or the one on the left)
        to: 11, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [],
        type: 3 //0,1,2,3,4
    },
    {
        from: 3, //the highest (or the one on the left)
        to: 12, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[24,1],[24,4]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 4, //the highest (or the one on the left)
        to: 13, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[31,1]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 5, //the highest (or the one on the left)
        to: 6, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [],
        type: 0 //0,1,2,3,4
    },
    {
        from: 5, //the highest (or the one on the left)
        to: 15, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[39,1]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 5, //the highest (or the one on the left)
        to: 16, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[43,2],[44,2]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 6, //the highest (or the one on the left)
        to: 7, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [],
        type: 0 //0,1,2,3,4
    },
    {
        from: 7, //the highest (or the one on the left)
        to: 17, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[56,1]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 8, //the highest (or the one on the left)
        to: 18, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[3,6],[1,6]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 8, //the highest (or the one on the left)
        to: 19, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[6,5],[6,6]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 9, //the highest (or the one on the left)
        to: 19, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[8,4]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 9, //the highest (or the one on the left)
        to: 20, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[11,4],[11,6]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 10, //the highest (or the one on the left)
        to: 11, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [],
        type:  0//0,1,2,3,4
    },
    {
        from: 10, //the highest (or the one on the left)
        to: 21, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[18,5],[18,6]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 10, //the highest (or the one on the left)
        to: 34, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[20,8],[21,8]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 11, //the highest (or the one on the left)
        to: 22, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[23,8]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 12, //the highest (or the one on the left)
        to: 23, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[27,4]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 13, //the highest (or the one on the left)
        to: 14, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [],
        type: 4/*6*///0,1,2,3,4 TODO
    },
    {
        from: 13, //the highest (or the one on the left)
        to: 23, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[29,3],[29,6]],
        type: 4/*6*///0,1,2,3,4 TODO
    },
    {
        from: 13, //the highest (or the one on the left)
        to: 24, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[31,5],[34,5]],
        type: 0/*4*///0,1,2,3,4 TODO
    },
    {
        from: 14, //the highest (or the one on the left)
        to: 15, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[38,3],[38,4]],
        type: 4/*6*///0,1,2,3,4 TODO
    },
    {
        from: 14, //the highest (or the one on the left)
        to: 25, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[35,4],[36,4]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 15, //the highest (or the one on the left)
        to: 16, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [],
        type: 4/*6*///0,1,2,3,4 todo
    },
    {
        from: 15, //the highest (or the one on the left)
        to: 26, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[39,5],[40,5]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 16, //the highest (or the one on the left)
        to: 28, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[44,6]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 16, //the highest (or the one on the left)
        to: 29, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[50,4]],
        type: 4/*6*///0,1,2,3,4 todo
    },
    {
        from: 17, //the highest (or the one on the left)
        to: 29, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[55,4],[55,5]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 17, //the highest (or the one on the left)
        to: 30, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[58,4]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 17, //the highest (or the one on the left)
        to: 43, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [],
        type: 3 //0,1,2,3,4
    },
    {
        from: 18, //the highest (or the one on the left)
        to: 31, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[1,9]],
        type: 3 //0,1,2,3,4
    },
    {
        from: 19, //the highest (or the one on the left)
        to: 32, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[8,8],[9,8]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 20, //the highest (or the one on the left)
        to: 33, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[12,7],[14,7],[14,8]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 21, //the highest (or the one on the left)
        to: 33, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[17,8]],
        type: 0 //0,1,2,3,4
    },
    {
        from: 23, //the highest (or the one on the left)
        to: 22, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[25,6],[25,7],[24,7]],
        type: 4/*6*///0,1,2,3,4
    },
    {
        from: 22, //the highest (or the one on the left)
        to: 34, //the lowest
        direction: 1, //0 if horizontal, 1 if vertical
        turns: [[24,9]],
        type: 4/*6*///0,1,2,3,4
    },
    {
        from: 22, //the highest (or the one on the left)
        to: 35, //the lowest
        direction: 0, //0 if horizontal, 1 if vertical
        turns: [[25,8]],
        type: 3 //0,1,2,3,4
    }
];
